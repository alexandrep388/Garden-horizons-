local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

-- [1] Création du thème Emerald
WindUI:AddTheme({
    Name = "Emerald",
    Accent = Color3.fromHex("#047857"),
    Background = Color3.fromHex("#011411"),
    Outline = Color3.fromHex("#FFFFFF"),
    Text = Color3.fromHex("#ecfdf5"),
    Placeholder = Color3.fromHex("#3fbf8f"),
    Button = Color3.fromHex("#059669"),
    Icon = Color3.fromHex("#10b981"),
})

WindUI:SetTheme("Emerald")

-- [2] Création de la fenêtre
local Window = WindUI:CreateWindow({
    Title = "Survive 100 day in sea",
    Icon = "panel-right",
    Author = "Gd studio",
    Folder = "Survive100dayinsea",
    Transparent = true,
    User = {
        Enabled = true,
        Anonymous = false,
        Callback = function()
            print("Profil cliqué")
        end,
    },
})

-- [3] Organisation des Sections et Onglets

-- Section principale
local MainGroup = Window:Section({
    Title = "Main",
    Icon = "cuboid",
    Opened = true,
})

-- On ajoute les onglets DANS MainGroup (Note l'utilisation de MainGroup:Tab au lieu de Window:Tab)
local TabInfo = MainGroup:Tab({
    Title = "INFO",
    Icon = "info",
    Locked = false,
})

local TabMain = MainGroup:Tab({
    Title = "MAIN",
    Icon = "bird",
    Locked = false,
})

-- Section secondaire
local SecondaryGroup = Window:Section({
    Title = "Secondary",
    Icon = "armchair",
    Opened = false,
})

local MainGroup = Window:Section({
    Title = "Bring",
    Icon = "cuboid",
    Opened = true,
})

-- On ajoute les onglets DANS MainGroup (Note l'utilisation de MainGroup:Tab au lieu de Window:Tab)
local TabOther = MainGroup:Tab({
    Title = "Other",
    Icon = "info",
    Locked = false,
})

local TabBring = MainGroup:Tab({
    Title = "Bring",
    Icon = "bird",
    Locked = false,
})

-- Section tertiaire
local ThirdsGroup = Window:Section({
    Title = "Thirds",
    Icon = "circle-plus",
    Opened = false,
})

---
--- EXEMPLE DE TEXTE GRADIENT DANS L'ONGLET MAIN
---

local PageSection = TabMain:Section({
    Title = "Fonctionnalités de Survie"
})

-- Application du dégradé sur le titre de la section de la page
PageSection:AddGradient(WindUI:Gradient({
    ["0"] = { Color = Color3.fromHex("#10b981"), Transparency = 0 },
    ["100"] = { Color = Color3.fromHex("#047857"), Transparency = 0 },
}, { Rotation = 90 }))

PageSection:Toggle({
    Title = "Auto-Farm",
    Value = false,
    Callback = function(state)
        print("Auto-farm est :", state)
    end
})

local Tab = Window:Tab({
    Title = "INFO",
    Icon = "bird", -- optional
    Locked = false,
})

local Tab = Window:Tab({
    Title = "MAIN",
    Icon = "bird", -- optional
    Locked = false,
})

local Section = Window:Section({
    Title = "Secondary",
    Icon = "armchair",
    Opened = true,
})

local Section = Window:Section({
    Title = "thirds",
    Icon = "circle-plus",
    Opened = true,
})
